### Instruction
You are given a database schema and sample rows. Write a single SQL query that answers the user's request. Ensure the query runs in SQLite and returns the exact result. If ambiguous, ask a clarifying question instead of guessing.

### Schema
{{schema}}

### Sample rows
{{samples}}

### User intent
{{nl}}

### Output format
Return only the SQL query. Do not include explanatory text.

### Reranking instructions (internal)
- Produce N=5 candidate SQL queries.
- For each candidate, run it against the DB snapshot.
- Prefer candidate with execution result identical to the gold (or highest result match score).
- If a candidate errors, include the error to help the model repair.